<<<<<<< HEAD
<div>
  <%= link_to user_path(@moment.user_id) do %>
  <img class="user" src="https://kitt.lewagon.com/placeholder/users/tgenaitay">
  <% end %>
=======
<div class="container">
  <div class="moment-main-info">
    <ul>
      <li><%= @moment.category %></li>
      <li><%= @moment.activity %></li>
      <li><%= @moment.description %></li>
      <li><%= @moment.location %></li>
    </ul>
  </div>
  <div class="moment-date">
    <ul>
      <li>
        Starting on <%= @formatted_start %>
      </li>
      <li>
        Ending on <%= @formatted_end %>
      </li>
    </ul>
  </div>

<%= link_to user_path(@moment.user_id) do %>
  <img class="user" src="assets/images/moment.photo.jpg">
<% end %>

<%= link_to 'Back to Moments List', moments_path, class: 'btn btn-info' %>
<%= link_to 'Bookings', moment_bookings_path(@moment), class: 'btn btn-success' %>
<%= link_to 'Join This Moment', new_moment_booking_path(@moment) %>
<% if @moment.user == current_user %>
  <%= link_to 'Edit Moment', edit_moment_path, :class => 'btn btn-primary' %>
  <%= link_to 'Delete this moment', moment_path, method: :delete, class: 'btn btn-danger' %>
<% end %>
>>>>>>> ef599c144f4b4a1a3475133bd82b1e572337c482
</div>
<div class="activity_description">
  <p>Category:
    <%= @moment.category %>
  </p>
  <p>Category:
    <%= @moment.activity.truncate(18, separator: /\s/) %>
  </p>
  <p>Description:
    <%= @moment.description.truncate(60, separator: /\s/) %>
  </p>
  <p>Location:
    <%= @moment.location %>
  </p>
  <p>From:
    <%= @moment.start_time %>h, To:
    <%= @moment.end_time %>h </p>
  </div>
  <div class="activity_buttons">
    <%= link_to 'Back to Moments List', moments_path, class: 'btn btn-info' %>
    <%= link_to 'Bookings', moment_bookings_path(@moment), class: 'btn btn-success' %>
    <%= link_to 'Join This Moment', new_moment_booking_path(@moment), class: 'btn btn-info' %>
    <% if @moment.user == current_user %>
    <%= link_to 'Edit Moment', edit_moment_path, :class => 'btn btn-primary' %>
    <%= link_to 'Delete this moment', moment_path, method: :delete, class: 'btn btn-danger' %>
    <% end %>
  </div>
